#include <iostream>
#include <vector>
#include <stdlib.h>
#include <string>
#include <algorithm>
#include <stdio.h>
#include <set>

using namespace std;


const int maxn = 200 * 1000;
long long adj[maxn + 100][maxn + 100];
pair<int, int> edg[maxn + 100];
int n, m;

int main()
{
	cin >> n >> m;
	for(int i = 0; i < n; i++
	{
		int x, y;
		scanf("%d %d", &x, &y)
	}
	
	
}
