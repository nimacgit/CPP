#include <iostream>
#include <vector>
#include <stdlib.h>
#include <string>
#include <algorithm>
#include <stdio.h>


using namespace std;

int n;
const int maxn = 1000 * 2;
int f[maxn + 100], s[maxn + 100];

int main()
{
	cin >> n;
	for(int i = 0; i < n ;i++)
		scanf("%d", &f[i]);
	for(int i = 0; i < n ;i++)
		scanf("%d", &s[i]);
	for(int i = 0; i < n; i++)
	{
		for(int j = f[])
	}
}