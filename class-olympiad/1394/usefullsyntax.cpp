	//make pair
	make_pair();
	//number of one in binery
	__builtin_popcount(20);
	//
	__builtin_popcountll(2000);
	//zero in binary
	__builtin_ctz(20);
	//leading zero in binary
	__builtin_clz(20);


	ferma :
	a % p = y;
	a / z % p = ?
	z' = z ^ (p - 2) % p
	ans = y * z' 


	long double pi = 3.141592653589793238462643383279502884197169399375105820974944592307816406286;
	cout.precision(5);
	cout << fixed;
	-Wall : warning
	-std=c++11;
	memset(a, 0, sizeof a)
	for(auto v : vector_name){};
	
	head -n 1 haas-problems/sponsor/tests/*.in
*/
	for()

	alias g++='g++ -std=c++11......'
	template <typename x, typename y>
	struct tank{
	int x, y, health; string s;
	tank(int x, int y) : x(x), y(y), health(100), s("hi"){}
	};

	string operator *(const string & s, int t) const
	{
		string r;
		{}
		return r;
	}

	
	tail
	head
	grep
	|
	jobs
	fg - bg
	ctrl-z
	ulimit
	perf





clean code
code complete 2.0
design patters(gang of four)



	.vimrc: set makeprg=g++\ %\ -O2\ -Wall\ -wextra
